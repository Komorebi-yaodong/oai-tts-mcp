# oai-tts-mcp 项目进度

> 项目：OpenAI 兼容 TTS MCP 服务（stdio）  
> 创建日期：2026-09-10  
> 包管理器：pnpm

## Done
- [x] 对齐 Code 专属记忆与用户需求。
- [x] 调研 OpenAI TTS：请求端点为 `POST /v1/audio/speech`；`gpt-4o-mini-tts` 支持 `instructions` 进行语气控制。
- [x] 明确音色查询兼容接口：`GET /v1/tts/voices?model={model}`。
- [x] 明确失败回退策略：上游接口失败、超时或返回无效 `voices[]` 时返回内置 OpenAI 音色。
- [x] 创建 TypeScript + pnpm + MCP stdio 工程骨架：`package.json`、`tsconfig.json`、`.gitignore`、`pnpm-lock.yaml`。
- [x] 实现 `list_voices`：请求 `/v1/tts/voices?model=...`，远程优先；失败时返回 13 个内置音色与 `source: "fallback"`、失败原因。
- [x] 实现 `text_to_speech`：请求 `/v1/audio/speech`；支持 `input`、`voice`、`instructions`、`speed`、`response_format`；默认 speed 为 `1.25`。
- [x] 实现 Base URL 规整、环境变量校验、60 秒超时、HTTP/空音频错误处理与安全错误摘要。
- [x] 初版直接返回 MCP `audio` 内容块（Base64 + MIME），后续根据客户端可用性改造为公开 URL 交付。
- [x] 创建 `README.md`、`env.example` 与 MCP 客户端配置示例；真实 Key 仅由进程环境变量注入。
- [x] 完成 Urusai multipart 上传：`POST https://api.urusai.cc/v1/upload`，发送 `file`、可选 `token` 和 `r18=0`；严格校验 `status=success` 与 `data.url_direct`。
- [x] `text_to_speech` 成功后仅返回 URL JSON/结构化内容：`status`、`url`、`model`、`voice`、`speed`、`format`、`mime_type`；不再返回 Base64 音频块。
- [x] 创建随机化上传文件名，文件名不包含用户文本；上传异常不回显 Token 或完整上游响应。
- [x] 完成 `pnpm build` 与 `pnpm check`。
- [x] 完成本机模拟集成测试：验证 `POST /v1/audio/speech`、默认 speed=`1.25`、默认 voice、`instructions` 透传及 `audio/mpeg` 返回块。
- [x] 完成本机 TTS + Urusai 模拟上传测试：验证 multipart、`r18=0`、Token 字段、随机文件名、URL JSON 与无 Base64 audio 块。

## Doing
- 无。

## Todo
- [ ] 在 MCP 客户端中填入真实的 `OAI_TTS_BASE_URL`、`OAI_TTS_API_KEY`、`OAI_TTS_MODEL`、`OAI_TTS_VOICE`、可选 `URUSAI_API_TOKEN` 后进行真实服务联调。
- [ ] 若需自有可控存储，可将 `URUSAI_UPLOAD_URL` 指向自建的兼容 multipart 上传服务。

## Notes / Issues
- 真实 API Key 和 `URUSAI_API_TOKEN` 仅从进程环境变量读取；不得写入仓库、日志或进度文档。
- Base URL 支持 `https://host` 或 `https://host/v1`，实现时会规整以避免生成 `/v1/v1/...`。
- Urusai 上传使音频离开本机并可能被持有 URL 的人访问；不得上传机密、隐私、个人信息或未获授权的内容。
- 内置回退音色包含：alloy、ash、ballad、coral、echo、fable、nova、onyx、sage、shimmer、verse、marin、cedar；实际可用性仍可能受上游模型限制。
- `tts-1` / `tts-1-hd` 的可用音色范围小于新模型；AI 应优先调用 `list_voices` 并采用远程返回的 `voice_id`。
- 项目当前不是 Git 仓库；并且本会话仅允许 Git 只读操作，未创建提交。
- pnpm 安装时提示 `esbuild` 构建脚本未获批准；本项目的 `pnpm build` 与 `pnpm check` 已通过，未受影响。
